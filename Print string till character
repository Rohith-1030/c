#include <stdio.h>
#include<stdlib.h>

int main() 
{
    char str[100];
    scanf("%s",str);
    char ch;
    scanf(" %c",&ch);
    for(int i=0;i<strlen(str);i++)
    {
        if(str[i]==ch)
        {
            break;
        }
        else{
            printf("%c",str[i]);
        }
    }
}

	
