#include <stdio.h>

int main() 
{
    int n;
    scanf("%d",&n);
    int sum=0;
    int temp=n;
    while(temp>0)
    {
        sum+=temp%10;
        temp/=10;
    }
    if(n%sum==0)
    {
        printf("Harshad Number");
    }
    else{
        printf("Not a Harshad Number");
    }
}
    
