#include <stdio.h>
#include<stdlib.h>
#include<string.h>

int main() 
{
    char str[100],s2[100];
    scanf("%s",str);
    int l=strlen(str),c=0;
    for(int i=0;i<l;i++)
    {
        if(str[i]=='a'||str[i]=='e'||str[i]=='i'||str[i]=='o'||str[i]=='u')
        {
            c++;
        }
    }
    printf("%d",c);
   
}
