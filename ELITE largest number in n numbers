#include <stdio.h>
int max(int x,int y){
    return x>y?x:y;
}

int main() 
{
    int n,maxv,curr;
    scanf("%d%d",&n,&maxv);
    for(int i=2;i<=n;i++){
        scanf("%d",&curr);
        maxv=max(maxv,curr);
    }
    printf("%d ",maxv);
}
    
	
