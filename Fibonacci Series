#include <stdio.h>
#include<math.h>
int main() 
{
    int n;
    scanf("%d",&n);
    int sum,a=0,b=1,next;
    for(int i=0;i<n;i++)
    {
        printf("%d ",a);
        next=a+b;
        a=b;
        b=next;
    }
    return 0;
}
